const myLibrary = [];

function Book(id) {
    // the constructor..
    this.id = id;
    this.book = {};
}

function addBookToLibrary(title,genre,description,author,pictureUrl,releaseDate) {
  // take params, create a book then a store it in the array
}

